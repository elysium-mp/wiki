name: Update wiki posts
on:
  workflow_dispatch:
  push:
    branches:
      - "main"
jobs:
  update-posts:
    runs-on: ubuntu-latest
    name: "Update posts" 
    steps:
      - name: Clone posts repository
        uses: actions/checkout@v3
        with:
          clean: true
          path: "posts"
          token: ${{ secrets.CI_TOKEN }}

      - name: Clone wiki repository
        uses: actions/checkout@v3
        with:
          clean: true
          repository: "cibucristi/elysium-wiki-website"
          path: "wiki"
          ref: "main"
          token: ${{ secrets.CI_TOKEN }}

      - name: Update posts
        if: always()
        shell: bash
        run: |
          rm -rf ./wiki/new_wiki
          mkdir ./wiki/new_wiki
          mv -f ./posts/* ./wiki/new_wiki
          mv -f ./wiki/new_wiki ./wiki
          git config --global user.email "cibucristi1@gmail.com"
          git config --global user.name "cibucristi"
          git status
          git add .
          git commit -m "Update posts with ${{ github.sha }}" || true
          git push || true
